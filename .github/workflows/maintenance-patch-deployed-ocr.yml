name: Patch deployed OCR verification

on:
  push:
    branches: [main]
    paths:
      - 'scripts/maintenance-patch-deployed-ocr.py'
      - '.github/workflows/maintenance-patch-deployed-ocr.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Patch OCR browser coverage
        run: python scripts/maintenance-patch-deployed-ocr.py

      - name: Commit test updates only
        run: |
          git diff --check
          git checkout -- .github/workflows/pages.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm scripts/maintenance-patch-deployed-ocr.py
          git add tests/deployed/preview.spec.ts tests/e2e/report-intake.spec.ts
          git commit -m 'test: harden local image ocr verification'
          git push
